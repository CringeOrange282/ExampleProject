# ExampleProject
###  1. Начало работы 

**Один раз на новом компьютере:**
* `git config --global user.name "Имя"` — Записать своё **имя** для подписи истории.
* `git config --global user.email "email"` — Записать свой **email** для подписи истории.

**При создании нового проекта с нуля:**
* `git init` — **Создать** новый пустой репозиторий в текущей папке.
* `git remote add origin <URL>.git` — **Привязать** эту папку к созданному GitHub-репозиторию.

**ИЛИ если проект уже есть на GitHub:**
* `git clone <URL>.git` — **Скачать** готовый репозиторий на компьютер.
* `cd <папка>` — **Войти** в скачанную папку через консоль для работы.

---

###  2. Сохранение и отправка
* `git pull origin <ветка>` — **Скачать** свежие изменения из GitHub перед началом работы.
* `git add .` — **Добавить** все измененные файлы к сохранению.
* `git add <файл>` — **Добавить** конкретный файл или папку.
* `git reset <файл>` — **Убрать** файл из сохранения (отмена `git add`).
* `git commit -m "текст"` — **сохранить** изменения локально.
* `git push origin <ветка>` — **Отправить** сохраненное на GitHub.

---

###  3. Работа с ветками
* `git branch <ветка>` — Просто **создать** новую ветку.
* `git checkout <ветка>` — **Перейти** на другую ветку.
* `git checkout -b <ветка>` — **Создать** ветку и **сразу перейти** в неё (заменяет две команды выше).
* `git branch -M main` — **Переименовать** текущую ветку в `main`.

---

###  4. Слияние веток (Merge)
* `git merge <имя_ветки>` — **Влить** указанную ветку в ту, в которой ты сейчас находишься.
* `git status` — **Проверика** состояния 
